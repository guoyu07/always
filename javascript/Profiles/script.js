$(window).load(function(){
    /*
    $('.view').click(function(){
        var row = $(this).parents('.pager-row')[0];
        var profile_id = $(row).data('rowId');
        document.location.href = 'always/admin/profiles/view?profile_id=' + profile_id;
    });

    $('.diff').click(function(){
        var row = $(this).parents('.pager-row')[0];
        var profile_id = $(row).data('rowId');
        document.location.href = 'always/admin/profiles/diff?profile_id=' + profile_id;
    });
    */
});